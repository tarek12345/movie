import type { BaseChannel } from '@sendbird/chat';
/**
 * Diff utils for channel
 * @param {BaseChannel} [a]
 * @param {BaseChannel} [b]
 * @returns {boolean}
 * */
export declare function isDifferentChannel<T extends BaseChannel>(a?: T, b?: T): boolean;
