import React from 'react';

import type { ReactMessageTemplateImageProps } from './index';

export const ImgReactMessageTemplateImage = (props: ReactMessageTemplateImageProps) => {
  return <img {...props} onError={props.onError} referrerPolicy="no-referrer" />;
};
