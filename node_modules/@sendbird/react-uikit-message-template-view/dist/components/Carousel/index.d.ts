import { ReactElement } from 'react';
import { type MessageTemplateProps } from '@sendbird/uikit-message-template';
import type { ReactParsedProperties } from '../../styles';
interface CarouselProps {
    children: ReactElement<MessageTemplateProps>[];
    style?: ReactParsedProperties;
    spacing?: number;
    maxChildWidth?: number;
}
export declare const Carousel: ({ children, style, spacing, maxChildWidth, }: CarouselProps) => ReactElement;
export default Carousel;
