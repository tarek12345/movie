import { SizeSpec, View } from '@sendbird/uikit-message-template';
export declare function isWrappedText(view: View): boolean;
export declare const isSizeCompatibleWithOption: ({ size, option, }: {
    size: SizeSpec;
    option: 'wrap' | 'fill' | 'fixed';
}) => boolean;
export declare const isGif: (url: string | undefined) => boolean;
export declare const isNumber: (val?: string | number | null) => val is number;
