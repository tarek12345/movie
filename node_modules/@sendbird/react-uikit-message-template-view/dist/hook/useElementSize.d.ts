/**
 * @returns an element's width / height observed by IntersectionObserver,
 * and ref to detect its resize event
 */
export declare function useElementSize(): {
    ref: import("./useRefEffect").EffectRef<HTMLElement>;
    width: number;
    height: number;
    contentWidth: number;
    contentHeight: number;
    paddingWidth: number;
    paddingHeight: number;
};
